❓ Ejercicio 3: Esto no tiene valor

Ya que vas entendiendo cómo se asignan las variables, te traemos algo para pensar: ¿qué pasa si intento usar una variable a la que nunca le asigné un valor?

Entonces, ¿es necesario darle valor a nuestras variables antes de usarlas?

🏋️ Ejercicio 4: Todo tiene un inicio

Para evitar errores, antes de utilizar una variable tenemos que darle un valor inicial, es decir, inicializarla.

Definí una función ascensor_sobrecargado, que toma una cantidad de personas y dice si entre todas superan la carga máxima:

ascensor_sobrecargado(1) → False

ascensor_sobrecargado(100) → True

Ah, y ¿cuál es el peso promedio de una persona? ¿Y la carga máxima? Para eso utilizaremos dos variables:

peso_promedio_persona_en_kilogramos, la cual ya está inicializada
carga_maxima_en_kilogramos que vas a tener que inicializar en 300
📥 Entrada y salida de datos

Ahora vamos a hacer que el programa sea más interactivo:

Pedile al usuario que ingrese la cantidad de personas que suben al ascensor (entrada de datos).


Ayuda: Usá una variable y el método input(). Recordá que la entrada es un texto, por lo que tenés que convertirla a número con int().
Ejemplo: personas = int(input("Ingrese la cantidad de personas: "))

Con ese valor, llamá a la función ascensor_sobrecargado.
Mostrá en pantalla si el ascensor está sobrecargado o no (salida de datos).

Ejemplo esperado en consola:

Ingrese la cantidad de personas: 5

¿El ascensor está sobrecargado? False